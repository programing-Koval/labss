int main(){ 
int radius = 5;
int l;
int s;
int v;
float znv = v; 
#define PI 3.14f
float r1  = 2 * (float) PI * (float) radius;
float r2  = PI  * (float) radius * (float) radius;
float r3  = 1.33 * (float) PI * (float) radius * (float) radius * (float) radi>
if(znv == l){
 znv = r1;
}else if(znv == s){
 znv = r2;
}else if(znv == v){ 
 znv = r3; 
}
return 0;
}
